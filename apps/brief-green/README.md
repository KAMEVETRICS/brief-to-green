# Brief→Green dashboard

Thin demo UI: **Brief → Bob Plan → parallel Agent/subagent lanes → Green**.

Uses mock plan JSON until IBM Bob 2.0 access unlocks at kickoff.

## Run

```bash
npm install
npm run dev
```

Dev server: http://localhost:5174
