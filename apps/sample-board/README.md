# Sample Board

Tiny Vite + React + TypeScript Kanban used as the **real/sample project** for Brief→Green.

## Intentional gap

Per `briefs/sample-feature-brief.md`, these are **not** implemented yet:

- Priority field + board-level priority filter
- Due-date badges (overdue / soon / upcoming)

Bob **Plan → Agent + subagents** should implement them during the hackathon.

## Run

```bash
npm install
npm run dev
```

Dev server: http://localhost:5173
