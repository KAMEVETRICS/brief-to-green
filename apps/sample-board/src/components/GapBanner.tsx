export function GapBanner() {
  return (
    <aside className="gap-banner" role="status">
      <strong>Feature gap (demo target)</strong>
      <span>
        Priority filter + due-date badges are not implemented. Drop the sample
        brief into Bob <em>Plan</em>, then fan out with Agent / subagents.
      </span>
    </aside>
  )
}
